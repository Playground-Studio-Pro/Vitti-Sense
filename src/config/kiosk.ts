export const KIOSK_FRAME = {
  // Configurable safe frame insets (pixels)
  top: 24,
  right: 28,
  bottom: 36,
  left: 28,

  // Uniform scale fallback (1 = original size)
  scale: 1,

  // Set to true to visually see the boundary and center lines for calibration
  showGuides: false,
};
